#test git hub commint
#test  2 commit note throught the internet
#class

hou.Node
node.name
node.setName
node.path

#print a tuple of the children of anyNode
for kid in anyNode.children()
print kid
#print parent node name
print anyNode.parent()

#create node child of the current node
box = ball.createNde(«box», «newbox»)
box.destroy()

#selection
ball.setSelected(True)
print ball.type().name()

#hou.node class
#creation et paramètres
ball= hou.node(«/obj/ball »)
ballTx =ball.parm(«tx»)
print ball.evalParm(« tx »)
for parm in ball.parm
	print parm.name()
ball.setParms({« tx:6»})

#input et output
merge = hou.node (mypath)
for input in merge.inputs()
	print input

#plug un mode dans un input
ball= hou.node(mypath)
red.setinput(0,hou.node(mypath)
red.copk(1,240,1)	force the node to cook
red.asCode()	#montre le code du node

#hou.Parm class
ty = hou.parmTuple(‘/onj/ball/t’)
ty.name()
ty.path()
ty.eval()	#retourne le part à la frame courante
ty.evalAtFrame(216)
ry=hou.parmTuple(‘mypath’)
ry.deleteAllKeyframes()
ry.set(45)
ry.setExpression(‘myexp’)

#hou.objNode
ball.setDisplayFlag(False)
ball.isDisplayFlagSet(False)
ball.setDislayFlag(True)
ball.isObjectDisplayed()

#matrice en python
parent = ball.parentAndSubnetTransform()
pre = ball.preTransform()
part = ball.parmTransform()

#hou.geometry class point and primitive data
pos = point.position()

#access attributes on geometry
red = hou.node(‘path’).geometry()
for attr in red.pointAttribs():
	print attr
red.saveToFile(‘path.geo’)

point = geo.createPoint() #cree un pas
point.setPosition((0,0,0))
 
poly = geo.createPolygon()
  
# Kwargs to access digital asset data
#call back
def onButtonPress(me,parm):

#python et digital assets
import toolutils
extra= toolutilscreateModuleFromSection('extra''ExtraPython')
def nodeId(node):

#python sop operator 



	-merger tout les abc d’un dossier dans la scène
	- export pour alembiv
	-lbrairy python à la racine du dossier de travail
	-










